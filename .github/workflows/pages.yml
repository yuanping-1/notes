# 推送到 main 后自动构建并发布到 GitHub Pages。
#
# 不需要 npm install：站点零第三方依赖，装个 Node 就能构建。
# 构建脚本会把产物写到临时目录再改名成交 dist/，所以这里的 path: dist 拿到的是完整产物。
name: Build and deploy to Pages

on:
  push:
    branches: [main]
  # 允许在 Actions 页面手动触发一次重新部署
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# 同一时刻只允许一个部署；不取消进行中的那次，避免线上出现半成品
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: 构建站点
        run: node src/build.js

      - name: 配置 Pages
        uses: actions/configure-pages@v5
        # 不开 enablement：创建 Pages 站点需要仓库 admin 权限，而 GITHUB_TOKEN 只有
        # 集成身份（Resource not accessible by integration），必然失败。Pages 由仓库设置或
        # 一次性 API 调用开启，这里只读配置即可。

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
